{
  "name": "authentication",
  "version": "0.0.1",
  "description": "Simple node.js authentication server",
  "main": "src/index.js",
  "scripts": {
    "pre-build": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "start": "node -r dotenv/config dist/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "server",
    "authentication"
  ],
  "author": "Dogyun Jeong",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  }
}
